route:
  group_by: ['alertname', 'symbol']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
  receiver: 'default-receiver'

receivers:
  - name: 'default-receiver'
    # Placeholder for webhook/slack/email
    # webhook_configs:
    #   - url: 'http://placeholder:5000/alert'
