# mljar-supervised Meta-Labeler Comparison — SDD progress

Base: 8abbd4aee04a00782d18e83bc1c39642606407ce
Plan: docs/superpowers/plans/2026-07-04-mljar-meta-labeler-comparison.md

Prior plans (BoostLSS XS Anomaly Detection, Jump-Diffusion/SHASH comparison,
Signal Improvements) completed and merged (PR #376, PR #377); their ledger
history is preserved in git log if needed.

- Task 1: complete (commits 8f8c68fa..db3a8d53, review clean after 1 fix; MAJOR FINDING: mljar's AutoML ensemble beats the baseline HistGradientBoostingClassifier on EURUSD — AUC 0.739->0.795, Option B +4.340->+4.689 bps/fill, n=1130 identical OOS rows, all 5 WFO folds succeeded 0 failures; SURPRISE: leaderboard shows Random Forest (not any boosting family) is the actual best individual base model in all 5 folds, Ensemble only narrowly beats it — all verified by reviewer directly against the diff's WFO fold logic (byte-identical to fit_meta_label_wfo), the paired OOS mask (symmetric, doesn't favor either classifier), and the pasted leaderboard numbers; fix: BACKLOG cited fold 2/4 numbers not shown in pasted leaderboards — added; EURUSD-only, scaling to 4 pairs is a follow-up decision; make quality clean)
- Task 2: 4-pair scale-up complete (run only, no code — script already supported `--pairs`). Re-ran `mljar_meta_labeler_compare.py --pairs EURUSD GBPJPY AUDUSD USDJPY` (q=0.90, window 4.5:5.5, tail_ratio, 90s/fold). All 4 raw trade populations reproduce established per-pair numbers exactly; baseline row reproduces the 4-pair tail_ratio result exactly (n=3865, AUC=0.773, +6.014 bps/fill) → clean apples-to-apples on identical OOS rows. RESULT: mljar ensemble beats baseline on full 4-pair set — AUC 0.773->0.805, Option B +6.014->+6.430 bps/fill (+0.416, +6.9%), 0/20 fold failures. EURUSD-only "RF is best base in every fold" finding does NOT generalize: RF is standout on EURUSD/GBPJPY/USDJPY but mid-pack on AUDUSD; what generalizes is "Ensemble wins or ties every fold" (20/20). Robust improvement is the AutoML ensemble itself, NOT a swap to plain RandomForestClassifier. Caveats: AutoML unseeded, mljar ephemeral, production fit_meta_label_wfo untouched. Written to BACKLOG "4-pair scale-up" subsection.
- Task 3: feature-importance complete. New script `mljar_feature_importance.py` (OOS permutation importance per WFO fold, 5 repeats, scored by AUC drop on the fitted AutoML ensemble's predict_proba — model-agnostic, tied to the OOS result, not in-sample SHAP). Bug found+fixed during run: `_TEMP_DIR` (`/tmp/mljar_explain_out`) was never created → all 4 pairs failed on first launch with "Cannot create directory"; added `os.makedirs(_TEMP_DIR, exist_ok=True)`, relaunched clean (exit 0). Mean OOS AUC reproduces comparison (AUDUSD 0.842, EURUSD 0.795, GBPJPY 0.828, USDJPY 0.770, pooled ~0.81 vs 0.805). KEY FINDINGS: (1) `rng_norm` (+0.118) and `oc` (+0.102) are the ONLY features doing real work — #1/#2 in every pair, mean/std ~1.9/2.0, an order of magnitude above the rest; the edge is entirely bar-shape (indecision=revert, directional=fail). (2) `ret_norm` distant pair-dependent #3 (only GBPJPY meaningful +0.050). (3) `tail_ratio` (PR #377 feature) contributes ~0 (pooled +0.0047, mean/std 0.46; only USDJPY faint +0.009) — the +0.056 bps/fill attributed to it is not reflected in OOS ranking power, economically marginal, model mostly ignores it. (4) `sigma_bps` unused/slightly-negative — redundant inside the meta-labeler because it already SELECTED the population (selection conditioned on). (5) all time/momentum features (hour/dow/mom_1/4/24) are noise. Reframe: the ensemble's +0.416 gain is a better fit on the SAME two shape signals, not new info — explains why RF was competitive (fits rng_norm×oc interaction well). Testable follow-up: leaner meta-labeler on rng_norm+oc(+ret_norm for GBPJPY) likely matches ensemble; dropping tail_ratio+sigma_bps should not hurt AUC. Written to BACKLOG "Feature importance" subsection.
